
# Default build options
buildopts -DNOPAM -DRELEASE_CRUNCH -DCRUNCHED_BINARY -DNOSECURE -DNOCRYPT -DNONETGRAPH -DNOIPSEC

progs cp sh test echo init mdconfig
progs mount mount_cd9660 mount_nullfs mount_unionfs umount sleep
# DEBUG
# progs ls kldstat

libs -ll -lutil -lkvm -lmd -lz
libs -ldisk -lufs -lkiconv 
libs -ledit 
libs -lncurses
libs -lcrypt

#libs -lmytinfo -lipx -lz -lpcap -lalias -lwrap
#libs -ledit -lutil -lmd -lcrypt -lmp -lm -lkvm
#libs -lgnuregex
