#!/bin/sh

## we do not need this since unionfs implementation

# if [ -d /usr/local/openssl ]; then
# mv /usr/local/openssl /usr/local/openssl_local
# mv /usr/local/openssl_local /var
# ln -s /var/openssl_local /usr/local/openssl
# fi

